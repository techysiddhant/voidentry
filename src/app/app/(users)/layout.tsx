const UserRoutesLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            <h1>User Routes Layout</h1>
            {children}
        </div>
    );
};

export default UserRoutesLayout